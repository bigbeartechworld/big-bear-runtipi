# Configuration for obsidian-livesync setup

# Name of the big-bear-obsidian-livesync application
name: big-bear-obsidian-livesync
# Service definitions for the big-bear-obsidian-livesync application
services:
  obsidian-livesync:
    # Name of the container
    container_name: obsidian-livesync
    # Image to be used for the container
    image: couchdb:3.5.0
    # Container restart policy
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      - obsidian-livesync_data_couchdb:/opt/couchdb/data
      - obsidian-livesync_data_local.ini:/opt/couchdb/etc/local.ini
    environment:
      - COUCHDB_USER=casaos
      - COUCHDB_PASSWORD=casaos
    # Ports mapping between host and container
    ports:
      # Mapping port 5984 of the host to port 5984 of the container
      - "5984:5984"
    labels:
      runtipi.managed: "true"
    networks:
      - tipi_main_network
volumes:
  obsidian-livesync_data_couchdb:
    name: obsidian-livesync_data_couchdb
    driver: local
  obsidian-livesync_data_local.ini:
    name: obsidian-livesync_data_local.ini
    driver: local
networks:
  tipi_main_network:
    external: true
