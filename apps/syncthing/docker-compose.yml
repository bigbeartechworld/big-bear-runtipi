# Configuration for syncthing setup

# Name of the big-bear-syncthing application
name: big-bear-syncthing
# Service definitions for the big-bear-syncthing application
services:
  syncthing:
    # Name of the container
    container_name: syncthing
    # Image to be used for the container
    image: syncthing/syncthing:2.0
    # Container restart policy
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      - syncthing_data:/var/syncthing
      - syncthing_media_data_syncthing:/media/data/syncthing
    # Ports mapping between host and container
    ports:
      - 8384:8384 # WebUI
      - 22000:22000/tcp # TCP file transfers
      - 22000:22000/udp # QUIC file transfers
      - 21027:21027/udp # Receive local discovery broadcasts
    # Environment variables for the container
    environment:
      - PUID=1000 # User ID
      - PGID=1000 # Group ID
    # Stop the container if it exits
    stop_grace_period: 1m
    labels:
      runtipi.managed: "true"
    networks:
      - tipi_main_network
volumes:
  syncthing_data:
    name: syncthing_data
    driver: local
  syncthing_media_data_syncthing:
    name: syncthing_media_data_syncthing
    driver: local
networks:
  tipi_main_network:
    external: true
