# Configuration for tugtainer setup

# Name of the big-bear-tugtainer application
# Service definitions for the big-bear-tugtainer application
services:
  tugtainer:
    # Name of the container
    container_name: tugtainer
    # Image to be used for the container
    image: quenary/tugtainer:v1.14.3
    # Container restart policy
    restart: unless-stopped
    # Ports to expose from the container
    ports:
      - "${APP_PORT}:80"
    # Environment variables for the container
    environment:
      TZ: "UTC"
      LOG_LEVEL: "warning"
      JWT_SECRET_KEY: "your-secret-key-here"
      ACCESS_TOKEN_LIFETIME_MIN: "5"
      REFRESH_TOKEN_LIFETIME_MIN: "43200"
      HTTPS: "FALSE"
    # Volumes to be mounted to the container
    volumes:
      - ${APP_DATA_DIR}/data:/tugtainer
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      runtipi.managed: "true"
    networks:
      - tipi_main_network
networks:
  tipi_main_network:
    external: true
