# Configuration for rackpeek setup

# Name of the big-bear-rackpeek application
# Service definitions for the big-bear-rackpeek application
services:
  rackpeek:
    # Name of the container
    container_name: rackpeek
    # Image to be used for the container
    image: aptacode/rackpeek:v2.0.0
    # Container restart policy
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      - ${APP_DATA_DIR}/config:/app/config
    # Ports mapping between host and container
    ports:
      # Mapping port 8095 of the host to port 8080 of the container
      - "${APP_PORT}:8080"
    labels:
      runtipi.managed: "true"
    networks:
      - tipi_main_network
networks:
  tipi_main_network:
    external: true
